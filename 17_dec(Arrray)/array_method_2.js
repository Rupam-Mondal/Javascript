let arr = [1 , 2  ,4 , 5 , 6]

//foreach goes every element to arrayy and executes the given call back



arr.forEach( (element) => { // will iterate to every array also we can pass index
    console.log(element * element);
})