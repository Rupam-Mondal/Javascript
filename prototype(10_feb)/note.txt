Map --> A Map does not contain any keys by default. 
It only contains what is explicitly put into it.


Obj --> An Object has a prototype, so it contains default keys that could collide with your
own keys if you're not careful.